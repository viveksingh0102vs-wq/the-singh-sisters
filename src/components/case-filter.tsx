"use client";import {useState} from "react";import {caseStudies} from "@/data/site";
export function CaseFilter(){const [filter,setFilter]=useState('All');const filters=['All',...Array.from(new Set(caseStudies.map(c=>c.industry)))];const shown=filter==='All'?caseStudies:caseStudies.filter(c=>c.industry===filter);return <><div style={{display:'flex',gap:9,flexWrap:'wrap',marginBottom:35}}>{filters.map(f=><button key={f} className={`btn ${filter===f?'btn-primary':'btn-secondary'}`} style={{padding:'9px 13px',width:'auto'}} onClick={()=>setFilter(f)}>{f}</button>)}</div><div className="grid cases" style={{gridTemplateColumns:'1fr 1fr'}}>{shown.map(c=><article className="card" key={c.title}><span className="pill">{c.industry} · {c.service}</span><h2 style={{fontSize:'1.65rem',margin:'18px 0'}}>{c.title}</h2><p><strong>Client:</strong> {c.client}</p><p><strong>Challenge:</strong> {c.challenge}</p><p><strong>Approach:</strong> {c.approach}</p><p><strong>Solution:</strong> {c.solution}</p><div style={{borderTop:'1px solid #e2e8f0',paddingTop:18,marginTop:18}}><strong style={{fontSize:'2.5rem',color:'#2563eb'}}>{c.metric}</strong> {c.label}</div></article>)}</div><style>{`@media(max-width:720px){.cases{grid-template-columns:1fr!important}}`}</style></>}
