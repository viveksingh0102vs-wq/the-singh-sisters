import type {MetadataRoute} from "next";const routes=['','/about','/services','/industries','/case-studies','/contact','/book-consultation','/privacy-policy','/terms'];export default function sitemap():MetadataRoute.Sitemap{return routes.map(route=>({url:`https://elevateconsulting.example${route}`,lastModified:new Date(),changeFrequency:route===''?'weekly':'monthly',priority:route===''?1:.7}))}
